<% provide(:title, 'All users') %>
<h1>Пользователи</h1>

<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
      <%= render user %>
  <% end %>
</ul>

<ul class="users">
  <%#= render @users %>
</ul>
<%= will_paginate %>